# AGENTS.md

## このリポジトリの目的
このリポジトリでは、造智機巧のアーキテクチャ文書そのものだけでなく、
その文書化方式、設計方針、開発手順、Git / GitHub 運用などの
メタアーキテクチャを日本語で整理する。

必ず既存文書を読んでから進めること。
特に最初に確認すること:
- `README.md`
- `inbox.md`
- `tasks.md`
- 関連する `decisions/*.md`
- 必要なら `decision_records/`

---

## 常設の基本方針
- 出力も文書編集も日本語で行う。
- まず既存文書・既存決定・既存記述を確認し、重複や矛盾を増やさない。
- 新しい仕組みや新しい文書を増やす前に、既存ファイルの追記や整理で済まないか確認する。
- 変更は小さく、局所的に、説明可能な単位で行う。
- ユーザーが書いた既存コメントや既存説明は、明確に不要と判断できる場合を除き削除しない。
- 自分が作っていない未コミット変更を上書き・巻き戻ししない。
- 推測で決め打ちしすぎず、重要な判断は根拠を文書に残す。

---

## このリポジトリでの作業サイクル
作業開始時は、以下をこの順で進めること。

### 1. inbox をトリアージする
`inbox.md` にある各項目について、
「task にするべきか」「今は task にしないか」を判断する。

task にする条件の目安:
- このリポジトリの目的に直接関係する
- 具体的な次の作業へ落とせる
- 調査・整理・比較・記述・決定準備として実行可能
- 先送りせず着手する価値がある

task にしない、または保留にする条件の目安:
- 重複している
- まだ曖昧で、質問しないと作業単位に落ちない
- すぐ進めるには大きすぎるので、まず論点整理が必要
- このリポジトリの範囲外
- 現時点では優先度が低すぎる

### 2. task 化する
task にすると決めた項目は:
- `inbox.md` から取り除く
- `tasks.md` に未完了チェックボックスとして追加する
- task 名は、曖昧な議題名ではなく「次にやる行為」が分かる文にする
- 大きすぎる項目は、最初の着手単位へ分割する

task の書き方:
- `- [ ] 〜する`
- できるだけ具体的かつ完了判定可能にする
- 1項目で複数の大仕事を背負わせない

### 3. task にしない場合の扱い
`inbox.md` の項目を task にしないと判断した場合も、
黙って捨てないこと。次のいずれかにすること。
- ユーザーへの返答で「task 化しない理由」を明示する
- 必要なら、より適切な表現に直して `inbox.md` に残す
- 単なる task ではなく決定事項に当たるなら `decisions/` へ反映する
- 非自明な判断経緯を残す価値があるなら `decision_records/` を追加する

「inbox から消したが、理由がどこにも残っていない」状態を作らないこと。

---

## 未完了 task の進め方
`tasks.md` のチェックが付いていない項目を確認し、
もっとも妥当な次の1件、または自然にまとまる少数件を進める。

進めるときの原則:
- まず関連文書を読む
- 既存方針と衝突しないか確かめる
- いきなり広げすぎず、今の task の範囲に集中する
- 途中で新しい論点が見つかっても、今の task と分離できるなら `inbox.md` に送る
- 完了した task は `- [x]` にする
- 一部しか終わらない場合は、終わった部分を反映し、残作業を新しい未完了 task へ分けてもよい

---

## ユーザーへの質問ルール
次のときはユーザーに質問してよい。むしろ質問した方がよい。
- 判断によって文書構造や長期方針が大きく変わる
- 複数案があり、優先基準がユーザー依存
- 根拠のない仮定で決めると後戻りが大きい
- 記述対象の境界やスコープが曖昧
- task 化するかどうかの判断に必要な情報が不足している

逆に、次のときは質問せず前進すること:
- 低リスクで後から修正しやすい
- 既存文書から妥当な推定ができる
- まず下書きや比較表や論点整理を作ることで前進できる

質問するときは、漠然と投げず、
「何が不明か」「どの選択肢があるか」「どれが作業結果に影響するか」を短く示すこと。

---

## 考えると長そうなことの扱い
作業中に、今すぐ深掘りすると長くなりそうな論点、
別 task に分けた方がよい論点、保留にした方がよい論点を見つけたら、
現在の task に無理に抱え込まず `inbox.md` に追加すること。

`inbox.md` に入れるべき典型例:
- 今の task の完了条件には不要だが、後で重要になる論点
- 判断材料が足りず、別途調査が要ること
- 設計をきれいにするための追加論点
- 今回見送るが、明らかに検討価値があること
- ユーザーに後で聞くべき質問候補

追加するときは、後から見て意味が分かる短い文にすること。

---

## decisions と decision_records の使い分け
### `decisions/`
次のような、現在有効な決定内容を書く:
- 文書体系の規則
- 配置ルール
- 採用するテンプレート
- 命名規則
- 運用方針
- 今後の作業の前提になる設計判断

### `decision_records/`
次のようなときだけ作る:
- なぜその決定にしたかが後で問題になりそう
- 比較した案と却下理由を残す価値が高い
- 一時点の判断経緯を履歴として残したい
- 自明ではない

自明なことまで濫造しないこと。

---

## 文書編集ルール
- 既存ファイルの構成・見出し・文体を尊重する
- 重複記述を増やさず、必要なら既存文を整理して統合する
- 変更したファイル間で整合性を取る
- 既存決定と矛盾する変更をする場合は、関連箇所をまとめて更新する
- タイトルや見出しだけ追加して中身が薄い文書を量産しない
- まだ決めていないことを、決定済みのように書かない
- 推測・案・決定を混同しない

---

## 作業完了時に満たすこと
1. `inbox.md` の対象項目について、task 化・保留・却下の扱いが明確になっている
2. `tasks.md` に未完了・完了の状態が正しく反映されている
3. 進めた task について、文書の変更が実際に入っているか、明確な blocker / 質問が示されている
4. 新しく見つかった長考事項や別件は `inbox.md` に逃がしてある
5. 必要なら `decisions/` または `decision_records/` が更新されている
6. ユーザーへの返答では、少なくとも次を簡潔に示す
   - inbox をどう判断したか
   - 何を task 化したか
   - 何を進めたか
   - 何を完了にしたか
   - 新たに inbox へ入れたもの
   - ユーザーに確認したいこと

---

## このリポジトリで避けること
- inbox の項目を、理由を残さず消す
- task を追加しただけで何も進めず終わる
- 1つの task に関係ない大改修を始める
- ユーザー確認が必要な分岐を勝手に確定する
- 現行文書との整合確認をしないまま新ルールを書く
- 決定事項と検討中事項を混ぜる